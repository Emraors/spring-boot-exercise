spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  cache:
    type: simple

# datasource and provider url are set dynamically by @DynamicPropertySource in the test

